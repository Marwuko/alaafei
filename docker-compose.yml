services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: alaafei
      POSTGRES_PASSWORD: alaafei
      POSTGRES_DB: alaafei
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
